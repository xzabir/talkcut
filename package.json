{
  "name": "talkcut",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Browser-based, transcript-driven video editor. All processing runs locally.",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.0.0",
    "jsdom": "^26.0.0",
    "typescript": "~6.0.2",
    "vite": "^8.1.1",
    "vite-plugin-pwa": "^1.0.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2",
    "webm-muxer": "^5.1.4"
  }
}
