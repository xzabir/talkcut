name: webcodecs-encode-engineer
description: |
  Specialist in WebCodecs encoding pipeline.
responsibilities:
  - Re-encode the provided VideoFrames to VP9 + Opus.
  - Mux the streams into a WebM container.
  - Provide progress updates (frame count, estimated time) to the UI.
  - Trigger download via File System Access API or fallback `<a>` tag.
allowed_skills:
  - chrome-only-feature-detection-fallback
